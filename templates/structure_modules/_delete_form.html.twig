<form method="post" action="{{ path('app_structure_modules_delete', {'id': structure_module.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ structure_module.id) }}">
    <button class="btn">Delete</button>
</form>
