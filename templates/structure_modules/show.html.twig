{% extends 'base.html.twig' %}

{% block title %}StructureModules{% endblock %}

{% block body %}
    <h1>StructureModules</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ structure_module.id }}</td>
            </tr>
            <tr>
                <th>Is_active</th>
                <td>{{ structure_module.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_structure_modules_index') }}">back to list</a>

    <a href="{{ path('app_structure_modules_edit', {'id': structure_module.id}) }}">edit</a>

    {{ include('structure_modules/_delete_form.html.twig') }}
{% endblock %}
